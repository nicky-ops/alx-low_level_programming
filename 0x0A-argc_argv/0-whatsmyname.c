#include <stdio.h>

/**
  * main - prints name of the program
  * @argc: integer to store number of arguments
  * @argv: array of char storing input parameters
  * Return: 0
  */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
